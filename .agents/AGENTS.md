# Workspace Rules

- All dependencies, virtual environments, packages, scripts, and output files must be installed and contained strictly inside this project directory (`C:\Users\varsh\Desktop\open ai`).
- Do not install global packages or write files outside this directory.
